diff --git a/app/globals.css b/app/globals.css
index 12d2f17..7ea23b6 100644
--- a/app/globals.css
+++ b/app/globals.css
@@ -129,6 +129,7 @@
     overflow: visible;
     padding-bottom: 1.25rem;
     line-height: 1.28;
+    min-height: 2.56em;
   }
 
   @media (min-width: 768px) {
diff --git a/tests/performance/pagespeed-minimal-fix.test.ts b/tests/performance/pagespeed-minimal-fix.test.ts
index 80ba56c..d1a260e 100644
--- a/tests/performance/pagespeed-minimal-fix.test.ts
+++ b/tests/performance/pagespeed-minimal-fix.test.ts
@@ -43,21 +43,20 @@ describe('R7 PageSpeed minimal zero-visual-change guards', () => {
     assert.match(globals, /@font-face\s*\{[\s\S]*?font-family:\s*'Noto Sans SC'[\s\S]*?size-adjust:\s*100%;[\s\S]*?ascent-override:\s*88%;[\s\S]*?descent-override:\s*12%;[\s\S]*?line-gap-override:\s*0%;[\s\S]*?\}/)
     assert.match(globals, /@font-face\s*\{[\s\S]*?font-family:\s*'Noto Serif SC'[\s\S]*?ascent-override:\s*88%;[\s\S]*?descent-override:\s*12%;[\s\S]*?line-gap-override:\s*0%;[\s\S]*?\}/)
     assert.match(globals, /\.site-hero-fullscreen\s*\{[\s\S]*?contain-intrinsic-size:\s*calc\(100vh - var\(--site-header-height\)\);/)
+    assert.match(globals, /\.site-hero-title\s*\{[\s\S]*?min-height:\s*2\.56em;[\s\S]*?\}/, 'hero H1 should reserve its two-line font-swap box to contain desktop CLS')
     assert.match(hero, /className="site-hero-title max-w-4xl/, 'hero title visual class contract must remain')
     assert.match(hero, />\s*Hermes Agent 中文站\s*</, 'hero title copy must remain unchanged')
   })
 
-  it('keeps unused legacy client JavaScript off the initial page while preserving infographic visuals', () => {
+  it('keeps homepage-specific connector JavaScript off the initial page while preserving infographic visuals', () => {
     const home = read('app/(marketing)/page.tsx')
-    const lazyConnectors = read('components/marketing/lazy-capability-connectors.tsx')
-    const connectors = read('components/marketing/capability-connectors.tsx')
 
-    assert.match(home, /<LazyCapabilityConnectorLayer\s+deferUntilIdle\s*\/?>/, 'non-critical connector JS should be opt-in idle loaded on the homepage')
-    assert.match(lazyConnectors, /deferUntilIdle\?: boolean/, 'lazy connector wrapper should expose an idle-defer switch')
-    assert.match(lazyConnectors, /requestIdleCallback|setTimeout\(loadConnectorLayer, 1600\)/, 'idle fallback should avoid contributing to first-screen JS work')
-    assert.match(lazyConnectors, /rootMargin:\s*deferUntilIdle \? '0px' : '600px 0px'/, 'idle-deferred mode should not pull connector JS far before the infographic')
-    assert.match(connectors, /data-connector-layer="dom-anchor"/, 'infographic connector visual layer must remain available')
+    assert.doesNotMatch(home, /LazyCapabilityConnectorLayer|lazy-capability-connectors/, 'homepage should not import or render connector client JS')
+    assert.match(home, /data-connector-layer="static-svg"/, 'infographic connector visual layer must remain as static SVG')
     assert.match(home, /data-connector-scope="capability-infographic"/, 'infographic DOM anchor scope must remain')
+    assert.match(home, /data-connector-node="core"/, 'core anchor contract must remain')
+    assert.match(home, /left-top/, 'left mechanism anchor contract must remain')
+    assert.match(home, /data-connector-node="right-bottom"/, 'right MCP anchor contract must remain')
   })
 
   it('keeps immutable CDN headers for preloaded font resources and does not weaken SEO or GA4 source markers', () => {
